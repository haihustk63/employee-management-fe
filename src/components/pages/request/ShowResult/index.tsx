import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const ShowResult = () => {
  return (
    <div>
      <Title className="app-title">Your request is being considered</Title>
    </div>
  );
};

export default ShowResult;
